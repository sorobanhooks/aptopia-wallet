{
  "name": "baku-api",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch run src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test --isolate",
    "tracer:digest-parity": "bun run scripts/tracer-digest-parity.ts",
    "tracer:swap-build": "bun run scripts/tracer-swap-build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^15.1.0",
    "hono": "^4.12.22"
  }
}
